# PassMini 开发日记

记录项目推进中的关键问题、处理方式和经验。

## 待补充

- [ ] ? 

## Bigram 训练效率
### 问题
使用双重循环统计 bigram 频率，计数速度非常慢。
```python
for inputs, targets in dataloader:
    for a, b in zip(inputs, targets):
        bigram.counts[a, b] += 1
```

### 处理
- **转移编码**: $a\to b = av+b$，其中 $v$ 是词表大小
- **张量化**: 编码后句子转为一阶张量（`pad` 单独掩码掉）
- **批量计数**: `torch.bincount` 统计出概率，再 `reshape`
```python
for inputs, targets in dataloader:
    # 掩码
    mask = (inputs != pad_id) & (targets != pad_id)
    # 转移编码
    encoded = inputs[mask] * vocab_size + targets[mask]
    # 批量计数
    counts = torch.bincount(encoded, minlength=vocab_size**2)
    bigram.counts += counts.reshape(vocab_size, vocab_size)
```

### 结果
700 万样本的训练，从 15 分钟降到 20 秒左右。

### 经验
少傻写 for 循环，能用框架并行尽量用。

## Best First Search 生成问题
### 问题
Best First Search 生成大量候选，但 EOS 少，pop 几十万条，堆里存了上千万条，闭合的密码才 20 个。

### 处理
主要问题在于候选数太多，而 EOS 生成概率又太低。采取以下三条措施，作为函数参数。

#### 1.长度归一化
候选分数 $s$，概率 $p$，前缀长度 $l$，惩罚系数 $\alpha$，按 $s$ 降序扩展
- 原本 $s = \log p$
- 改为 $s = \frac{\log p}{l ^ \alpha}$

$s$ 总是负数，所以 $\alpha$ 越大，越能缓解长序列的天然概率劣势。

> **详细推导**
> 
> 设每个 token 平均贡献是 $c$，则: 
> $$s = \sum^i \log p_i = cl$$
> $$s' = \frac{cl}{l^\alpha} = cl^{1-\alpha}$$
> 因此当 $\alpha=1$ 时 $s'=c$，理论上与长度无关。

#### 2.节点 top-k
每个节点限制展开的数量为 $k$。

最大长度 $l$，词表大小 $v$，候选数由 $v^l$ 降为 $k^l$。

可能导致错过全局最高概率，不过无所谓。

#### 3.层束宽
每一层扩展的候选数不超过束宽 $w$。

候选数进一步降为 $w \cdot l$。

### 结果
最后取 $\alpha=1, k=5$（没用 $w$ 因为剪的太极端了），pop 3 万条，堆里十几万条时，就能产出 1 万个密码。

## 批量生成问题
### 问题
模型的 `generate()` 方法仅支持单样本，随机采样非常慢，生成 1 万条就需要 2 分钟左右。
```python
outputs = []
for _ in range(max_step):
    probs = model(x) # (v,)
    x = probs.argmax(dim=-1) # (1,)
    outputs.append(x)
```

### 处理
使用带批量维度的张量生成，每个时间步对批量同一采样，EOS 的做掩码。
```python
outputs = []
for _ in range(max_step):
    probs = model(x)  # (b, v)
    x = probs.argmax(dim=-1)  # (b, 1)
    # 掩码 EOS
    mask |= (x == eos_id)
    outputs.append(x[~mask])  # 只保留未结束的样本
```

### 结果
批量大小取 50000，生成 100 万条密码只花了 2 分钟（不过显存快寄了）。