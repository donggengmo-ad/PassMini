# PassMini 开发日记

记录项目推进中的关键问题、处理方式和经验。

## Bigram 训练效率
### 问题
使用双重循环统计 bigram 频率，计数速度非常慢。
```python
for inputs, targets in dataloader:
    for a, b in zip(inputs, targets):
        bigram.counts[a, b] += 1
```

### 处理
- **转移编码**: $a\to b = av+b$，其中 $v$ 是词表大小
- **张量化**: 编码后句子转为一阶张量（`pad` 单独掩码掉）
- **批量计数**: `torch.bincount` 统计出概率，再 `reshape`
```python
for inputs, targets in dataloader:
    # 掩码
    mask = (inputs != pad_id) & (targets != pad_id)
    # 转移编码
    encoded = inputs[mask] * vocab_size + targets[mask]
    # 批量计数
    counts = torch.bincount(encoded, minlength=vocab_size**2)
    bigram.counts += counts.reshape(vocab_size, vocab_size)
```

### 结果
700 万样本的训练，从 15 分钟降到 20 秒左右。

### 经验
少傻写 for 循环，能用框架并行尽量用。

## Best First Search 生成问题
### 问题
Best First Search 生成大量候选，但 EOS 少，pop 几十万条，堆里存了上千万条，闭合的密码才 20 个。

### 处理
主要问题在于候选数太多，而 EOS 生成概率又太低。采取以下三条措施，作为函数参数。

#### 1.长度归一化
候选分数 $s$，概率 $p$，前缀长度 $l$，惩罚系数 $\alpha$，按 $s$ 降序扩展
- 原本 $s = \log p$
- 改为 $s = \frac{\log p}{l ^ \alpha}$

$s$ 总是负数，所以 $\alpha$ 越大，越能缓解长序列的天然概率劣势。

> **详细推导**
> 
> 设每个 token 平均贡献是 $c$，则: 
> $$s = \sum^i \log p_i = cl$$
> $$s' = \frac{cl}{l^\alpha} = cl^{1-\alpha}$$
> 因此当 $\alpha=1$ 时 $s'=c$，理论上与长度无关。

#### 2.节点 top-k
每个节点限制展开的数量为 $k$。

最大长度 $l$，词表大小 $v$，候选数由 $v^l$ 降为 $k^l$。

可能导致错过全局最高概率，不过无所谓。

#### 3.层束宽
每一层扩展的候选数不超过束宽 $w$。

候选数进一步降为 $w \cdot l$。

### 结果
最后取 $\alpha=1, k=5$（没用 $w$ 因为剪的太极端了），pop 3 万条，堆里十几万条时，就能产出 1 万个密码。

## 批量生成问题
### 问题
模型的 `generate()` 方法仅支持单样本，随机采样非常慢，生成 1 万条就需要 2 分钟左右。
```python
outputs = []
for _ in range(max_step):
    probs = model(x) # (v,)
    x = probs.argmax(dim=-1) # (1,)
    outputs.append(x)
```

### 处理
使用带批量维度的张量生成，每个时间步对批量同一采样，EOS 的做掩码。
```python
outputs = []
for _ in range(max_step):
    probs = model(x)  # (b, v)
    x = probs.argmax(dim=-1)  # (b, 1)
    # 掩码 EOS
    mask |= (x == eos_id)
    outputs.append(x[~mask])  # 只保留未结束的样本
```

### 结果
批量大小取 50000，生成 100 万条密码只花了 2 分钟（不过显存快寄了）。

## 模型批量评分
### 问题
TCN 和 Transformer 给每个密码单独算 logprob，父类默认批量计算函数也是傻傻串行。

### 处理
先将全部文本整理为 `[B,L]` teacher-forcing batch，一次得到 `[B,L,V]` logits；再取真实目标 token、屏蔽 PAD 并按序列求和：

$$\log P(x)=\sum_t \mathbf{1}[y_t\ne PAD]\log P(y_t\mid y_{\lt t})$$

```python
logits = model(inputs) # (B, L) -> (B, L, V)
# 取真实 token 的 logprob
token_lp = log_softmax(logits).gather(targets) # (B, L)
# 屏蔽 PAD, 按序列求和
logprob = token_lp.masked_fill(pad, 0).sum(dim=-1) # (B,)
```

### 结果
模型平均计算 密码数/sec 从 1400 提升到 3.6 万

### 经验
批量化不仅与 forward 有关，也与推理接口算法有关。

## Beam Search 批量推理
### 问题
Beam Search 对每个父节点、每个子 token 单独前推，而且在全局剪枝前就计算了大量最终会被丢弃的状态。

### 处理
同一深度的缓存形状一样。先批量算 `[B,V]` 分数，全局剪枝，再复制保留节点的父缓存，一次前推下一层：

```python
logprobs = masked_log_softmax(frontier.next_logits)  # (B, V)
# 展开所有状态
child = expand_metadata(logprobs) # (B, V) -> (B*V,)
# 全局剪枝
kept = global_top_k(child, beam_width) # (Beam, 2) 2 是 score, index
# 复制父缓存，前推下一层
parents = model.select_state(frontier, kept.parent_indices)
frontier = model.advance_state(parents, kept.token_ids)
```

### 结果
CPU 上 `beam_width=8` `max_length=6`逐节点版本 `0.0803s`，批量版本 `0.00305s`，约 `26.29x`


## Best-First Search 批量展开与弹堆
### 问题
严格 Best-First 每次只弹出一个堆节点，兄弟分支也逐个前推。

### 处理
#### 批量前推
一个父节点出堆时，他的所有子节点的“父缓存”一样，可以一次性前推。

#### 批量弹堆
同堆里批量弹多个节点，一块前推，从所有生成的子节点里筛选、入堆。这样会打乱部分出堆顺序，不再是严格 best first。

```python
# 弹出 batch_size 个节点
popped = [heappop(queue) for _ in range(batch_size)]
# 找出所有弹出节点的子 token
for nodes in group_by_depth(popped):
    # 将同一深度的父节点缓存堆叠
    parents = model.stack_states(nodes)
    # 批量前推生成子节点
    children = model.advance_state(repeat(parents), child_token_ids)
    # 将生成的子节点入堆
    heappush_all(queue, children)
```

### 结果
百万预算搜索，3 分钟完成，忘了以前多少了，估计 20 多分钟把。

### 经验
为了加速，可以牺牲一点严格的最优性，之前给 best first 加 beam 的时候就干过一次了。

## 随机评测流式统计
### 问题
随机评测先保存全部生成密码，再计算合法率和覆盖率，采样量一亿时占用内存太多。

### 处理
每个 batch 生成完立即累计合法数、不同合法密码和命中，在检查点保存覆盖率。Notebook 里同时按模型设置 batch，并且在模型切换时记录和清理 CUDA 空闲缓存。

- 总样本数为 $N$
- batch 为 $B$
- 不同合法密码数为 $U$
- 命中数为 $H$
- 检查点间隔为 $C$ 

主要内存由 $O(N)$ 降为 $O(B+U+H+N/C)$。

### 结果
用 5 万条重复样本小测，内存峰值从 `1.011 MiB` 降到 `0.025 MiB`（约 `41.15x`）。